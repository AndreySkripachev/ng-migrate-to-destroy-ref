{
  "$schema": "http://json-schema.org/draft-07/schema",
  "$id": "DestroyRef",

  "title": "My Full Schematics Schema",
  "type": "object",
  "properties": {
    "decoratorAlias": {
      "type": "string",
      "default": "Destroyable",
      "x-prompt": "Name of `Destroyable` decorator (or which you are use)?"
    },
    "name": {
      "type": "string",
      "default": "takeUntilDestroy",
      "x-prompt": "Name of `takeUntilDestroy` function (or which you are use)?"
    }
  },
  "required": ["name"]
}
